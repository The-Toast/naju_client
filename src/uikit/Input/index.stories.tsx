import StyledInput from '.'

import type { Meta, StoryObj } from '@storybook/nextjs-vite'

export default {
  title: 'Uikit/Input',
  component: StyledInput,
} as Meta<typeof StyledInput>

type Story = StoryObj<typeof StyledInput>

export const Primary: Story = {}